[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "knowledge-management-agent"
version = "0.1.0"
description = "A general knowledge management agent demo for RAG, workflow orchestration, multi-source perception, and asynchronous execution."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "langchain-core>=0.3,<0.4",
  "langchain-openai>=0.3,<0.4",
  "langgraph>=0.2,<0.4",
  "playwright>=1.52,<2",
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.2,<3",
  "python-dotenv>=1.0,<2",
  "requests>=2.32,<3",
  "httpx>=0.27,<1"
]

[project.scripts]
knowledge-agent = "xhs_interview_answer_copilot.main:main"
xhs-copilot = "xhs_interview_answer_copilot.main:main"

[project.optional-dependencies]
dev = [
  "mypy>=1.10,<2"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
xhs_interview_answer_copilot = ["tools/*.swift"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["xhs_interview_answer_copilot"]
